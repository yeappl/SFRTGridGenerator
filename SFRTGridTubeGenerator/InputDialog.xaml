<Window x:Class="VMS.TPS.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Grid Tube/Sphere Generator for SFRT"
        Width="580"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FontSize="13">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="0,6,8,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,6,0,0" />
            <Setter Property="Padding" Value="4,3,4,3" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,6,0,0" />
            <Setter Property="Padding" Value="4,3,4,3" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Width" Value="90" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Margin" Value="8,0,0,0" />
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Margin" Value="0,6,16,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Target ROI:" />
        <ComboBox x:Name="RoiComboBox"
                  Grid.Row="0" Grid.Column="1"
                  IsEditable="False" />

        <Label Grid.Row="1" Grid.Column="0" Content="Shape:" />
        <StackPanel Grid.Row="1" Grid.Column="1"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
            <RadioButton x:Name="TubeRadioButton"
                         Content="Tube"
                         GroupName="ShapeGroup"
                         IsChecked="True" />
            <RadioButton x:Name="SphereRadioButton"
                         Content="Sphere"
                         GroupName="ShapeGroup" />
        </StackPanel>

        <Label Grid.Row="2" Grid.Column="0" Content="Diameter (mm):" />
        <TextBox x:Name="DiameterTextBox"
                 Grid.Row="2" Grid.Column="1"
                 Text="5" />

        <Label Grid.Row="3" Grid.Column="0"
               Content="Tube/Sphere gap, boundary-to-boundary (mm):"
               ContentStringFormat="{}{0}"
               Padding="0"
               HorizontalContentAlignment="Left"
               VerticalContentAlignment="Center" />
        <TextBox x:Name="GapTextBox"
                 Grid.Row="3" Grid.Column="1"
                 Text="5" />

        <Label Grid.Row="4" Grid.Column="0" Content="Boundary margin (mm):" />
        <TextBox x:Name="BoundaryMarginTextBox"
                 Grid.Row="4" Grid.Column="1"
                 Text="5" />

        <TextBlock Grid.Row="6" Grid.ColumnSpan="2"
                   Margin="0,10,0,0"
                   TextWrapping="Wrap"
                   Foreground="Gray"
                   FontSize="11"
                   Text="IMPORTANT: please review generated GRID_TUBE/SPHERE geometry slice-by-slice before clinical use. 
                   &#10;Please report bugs to NCCS DRO Physics or email yeap.ping.lin@nccs.com.sg. 
                   &#10;Version 1.2.0, updated Sep 2026." />

        <StackPanel Grid.Row="8" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="GenerateButton"
                    Content="Generate"
                    IsDefault="True"
                    Click="OnGenerateClicked" />
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    IsCancel="True"
                    Click="OnCancelClicked" />
        </StackPanel>
    </Grid>
</Window>
